# 层层加码 (Laymask)

一款桌面图像蒙版合成工具——上传底图，绘制选区，将平铺图案填充到选区中，导出合成结果。

## 功能

- **多种选区工具** — 矩形、椭圆、画笔、橡皮擦
- **多层编辑** — 最多 10 个独立图层，各自维护图案和参数
- **图案填充** — 平铺 PNG/JPG 图案，支持缩放（10%–300%）、不透明度（5%–100%）、旋转
- **文字图案** — 自定义文字生成图案，可选字体与样式
- **模糊图案** — 对底图应用高斯模糊作为填充图案
- **历史图案** — 自动记录最近使用的图案，快速复用
- **撤销/重做** — 最多 20 步操作历史
- **多格式支持** — PNG、JPG、BMP、GIF、WebP
- **拖拽导入** — 直接拖放图片文件到窗口
- **文件关联** — 双击图片文件即可用 Laymask 打开

## 快捷键

| 按键 | 功能 |
|------|------|
| `R` | 矩形选区 |
| `E` | 椭圆选区 |
| `B` | 画笔 |
| `X` | 橡皮擦 |
| `V` | 移动图案 |
| `A` | 全选 |
| `Ctrl+Z` | 撤销 |
| `Ctrl+Shift+Z` | 重做 |

## 开发

```bash
npm install        # 安装依赖
npm run tauri:dev  # 开发模式
npm run tauri:build # 生产构建
```

## 技术栈

- **Tauri v2** — 跨平台桌面框架（Rust 后端）
- **HTML5 Canvas** — 5 层离屏画布合成管线
- **Vanilla JavaScript** — 无框架依赖
- **Vite** — 构建工具

## 项目结构

```
├── index.html          # 入口页面，工具栏 + 画布 + 图层面板
├── main.js             # DOM 事件绑定、UI 逻辑
├── canvas-engine.js    # 渲染引擎：离屏画布、鼠标/触摸事件、坐标映射
├── layer.js            # 图层系统
├── style.css           # 样式（暗色主题）
├── resource/           # 图标等静态资源
└── src-tauri/          # Tauri 后端（Rust）
    ├── src/            # Tauri 命令与插件配置
    ├── tauri.conf.json # 应用配置
    └── Cargo.toml      # Rust 依赖
```

## 渲染管线

```
底图 → 蒙版层(离屏A) → 图案层(离屏B) → source-in 合成 → 叠加到底图
```

1. 在主画布绘制底图
2. 在离屏画布 A 上绘制黑色选区形状（蒙版）
3. 在离屏画布 B 上用 `createPattern` 平铺填充整个画布
4. 对离屏画布 B 设置 `globalCompositeOperation = 'source-in'`，绘制离屏画布 A——图案被蒙版裁剪，PNG 透明度保留
5. 将合成结果绘制到主画布上

## License

MIT
